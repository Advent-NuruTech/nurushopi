import type { Route } from "next";

export const PICKUP_LOGIN_PATH = "/pickup/login" as Route;
export const PICKUP_DASHBOARD_PATH = "/pickup/dashboard" as Route;
export const PICKUP_POLICY_PATH = "/pickup-station-policy" as Route;
export const PICKUP_AGENT_TERMS_PATH = "/pickup-agent-terms" as Route;
